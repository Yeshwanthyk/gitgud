{
  "name": "gitgud",
  "version": "0.1.0",
  "type": "module",
  "bin": "./dist/cli.js",
  "scripts": {
    "dev": "bun --watch src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target bun",
    "check": "biome check .",
    "test": "bun test",
    "format": "biome format . --write"
  },
  "dependencies": {
    "giget": "^2.0.0",
    "yaml": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@biomejs/biome": "^1.9.0"
  }
}
